<Window x:Class="MP3Converter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Youtube Medya Dönüştürücü" Height="460" Width="580" 
        Background="#121212" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Icon="/mp3_cevirici_ikon.ico"
        Activated="Window_Activated"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}" Margin="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3D3D3D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border CornerRadius="12" Background="#1E1E1E" BorderBrush="#2D2D2D" BorderThickness="1">
                                            <Path x:Name="Arrow" Fill="#AAAAAA" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" Data="M0,0 L4,4 L8,0 z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,25,3" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="#252525" BorderThickness="1" BorderBrush="#2D2D2D" CornerRadius="10" Margin="0,5,0,0" Width="{Binding ActualWidth, ElementName=ToggleButton}">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center">

                <Grid Margin="0,0,0,5">
                    <TextBlock Text="🎵 Youtube Medya Dönüştürücü" FontSize="26" FontWeight="Bold" Foreground="#FF0000" HorizontalAlignment="Center"/>
                    <Button x:Name="btnInfo" Content="i" Width="26" Height="26" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" FontWeight="Bold" Cursor="Hand" Click="btnInfo_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                                <Setter Property="BorderBrush" Value="#2D2D2D"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="13" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2D2D2D"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" CornerRadius="12" Background="#1E1E1E" BorderBrush="#2D2D2D" BorderThickness="1" Margin="0,0,10,0">
                        <TextBox x:Name="txtUrl" Height="40" FontSize="14" VerticalContentAlignment="Center" HorizontalContentAlignment="Left" Padding="15,0,10,0"
                                    Background="Transparent" Foreground="White" BorderThickness="0" CaretBrush="White" TextChanged="txtUrl_TextChanged"/>
                    </Border>

                    <ComboBox x:Name="cb" Grid.Column="1" Height="40" FontSize="14" Cursor="Hand" SelectedIndex="0" SelectionChanged="cb_SelectionChanged">
                        <ComboBoxItem x:Name="cbMp3" Content="Ses / Mp3"/>
                        <ComboBoxItem x:Name="cbMp4" Content="Video / Mp4"/>
                    </ComboBox>
                </Grid>

                <Border x:Name="previewPanel" Background="#1E1E1E" CornerRadius="12" Margin="0,0,0,15" Visibility="Collapsed" Height="80">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" CornerRadius="10" Margin="5">
                            <Image x:Name="imgThumbnail" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,10,0">
                            <TextBlock x:Name="lblVideoTitle" Text="Bilgiler çekiliyor..." Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="lblVideoChannel" Text="Lütfen bekleyin..." Foreground="#AAAAAA" FontSize="12" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="lblVideoDuration" Text="Süre: --:--" Foreground="#888888" FontSize="12" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,5,0">
                        <Button x:Name="btnDownload" Content="MP3 Olarak İndir" Height="45" FontSize="15" FontWeight="SemiBold" Cursor="Hand" Click="btnDownload_Click" Visibility="Visible">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#FF0000"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#CC0000"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Background" Value="#252525"/>
                                                        <Setter Property="Foreground" Value="#666666"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button x:Name="btnStop" Content="⏹ İşlemi Durdur" Height="45" FontSize="15" FontWeight="SemiBold" Cursor="Hand" Click="btnStop_Click" Visibility="Collapsed">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#CC3333"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FF3333"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <Button x:Name="btnOpenFolder" Grid.Column="1" Content="📂 İndirilenleri Aç" Height="45" FontSize="15" FontWeight="SemiBold" Cursor="Hand" Click="btnOpenFolder_Click" Margin="5,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#3D3D3D"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#1A1A1A"/>
                                                    <Setter Property="Foreground" Value="#444444"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <Grid Margin="0,20,0,0" Height="28">
                    <ProgressBar Name="statusBar" Minimum="0" Maximum="100" Value="0" Background="#FF2D2D2D" Foreground="#FF0000" BorderThickness="0">
                        <ProgressBar.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </ProgressBar.Resources>
                    </ProgressBar>
                    <TextBlock x:Name="lblStatus" Text="Link yapıştırılmaya hazır." FontSize="13" FontWeight="SemiBold" Foreground="#FFA500" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Grid>

        <Grid x:Name="infoOverlay" Background="#D9000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#2D2D2D" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="25" Width="480">
                <StackPanel>
                    <TextBlock Text="🛠️ Uygulama Rehberi" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <TextBlock Text="🔗 Akıllı Link Paneli:" Foreground="#1DB954" FontWeight="Bold" FontSize="14" Margin="0,0,0,3"/>
                    <TextBlock Text="Kopyaladığınız YouTube linkini siz programa döndüğünüz an otomatik yakalar. Tekli video veya devasa Playlist'leri kendi algılar." Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,12" LineHeight="18"/>

                    <TextBlock Text="⚙️ Optimize Formatlar:" Foreground="#1DB954" FontWeight="Bold" FontSize="14" Margin="0,0,0,3"/>
                    <TextBlock Text="• Ses (Mp3): Ham sesi çeker, cihazlar için en ideal kalite olan 160kbps değerine boyutunu küçülterek (optimize) sıkıştırır." Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,5" LineHeight="18"/>
                    <TextBlock Text="• Video (Mp4): Kaliteyi bozmadan, cihazlarda gereksiz yer kaplamayan akıllı 720p formatını bulur ve indirir." Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,12" LineHeight="18"/>

                    <TextBlock Text="🛡️ Akıllı Koruma Sistemi:" Foreground="#1DB954" FontWeight="Bold" FontSize="14" Margin="0,0,0,3"/>
                    <TextBlock Text="Program her açılışta motorunu günceller. İndirmeyi iptal ederseniz diskinizde çöp dosya bırakmaz, anında temizler." Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="18"/>

                    <Button Content="Anladım, Kapat" Height="40" FontSize="14" FontWeight="SemiBold" Cursor="Hand" Click="btnCloseInfo_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#3D3D3D"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="messageOverlay" Background="#D9000000" Visibility="Collapsed" Panel.ZIndex="105">
            <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#2D2D2D" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="25" Width="380">
                <StackPanel>
                    <TextBlock x:Name="lblMessageTitle" Text="⚠️ Uyarı" Foreground="#FFA500" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="lblMessageText" Text="Mesaj buraya gelecek." Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,20" TextAlignment="Center" FontSize="14" LineHeight="20"/>

                    <Button Content="Tamam" Height="40" Width="140" FontSize="14" FontWeight="SemiBold" Cursor="Hand" Click="btnMessageOk_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#3D3D3D"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>